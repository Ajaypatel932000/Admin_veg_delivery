<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin_veg_delivery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Dashboad"
            android:label="Dashboard"/>
        <activity android:name=".OderActivity"
            android:label="Oders"
            android:parentActivityName=".Dashboad"></activity>
        <activity android:name=".Item_listActivity"
            android:label="Vegetable List"
            android:parentActivityName=".Dashboad"/>
        <activity android:name=".Add_itemActivity"
            android:label="Add Item"
            android:parentActivityName=".Dashboad"/>
    </application>

</manifest>